#include <iostream>

int main()
{
	/*
	int num = 10;	// 변수 초기화
	int num1;		// 변수 선언
	num1 = 20;		// 변수 대입
	num = 30;
	printf("int의 크기: %d\n", sizeof(int));

	char ch;		// 문자타입 
	printf("ch크기: %d\n", sizeof(char));

	double d = 3.14; // 실수타입
	printf("doubl크기: %d\n", sizeof(double));

	return 0;
	*/

	char ch = 0x7f;		// 0x는 16진법 0 ~ 9, A(10), B(11), C(12), D(13), E(14), F(15)
						// 0b 0(최상위 비트, 부호 비트 기능을 가지고 있음. 1이면 음수 0이면 양수)111 1111 (8bit) - 8 4 2 1
	return 0;
}