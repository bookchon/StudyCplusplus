#include <iostream>
using namespace std;

class mycalc {
private:
	int num1;
	int num2;
public:
	Mycalc(int n1, int n2) : num(n1), num(n2) {

	}
		int add(x + y);
		int sub(x - y);
		int mul(x * y);
		int div(x / y);

};

Mycalc(int n1, int n2) : num(n1), num(n2) {

}

int main()
{
	return 0;
}